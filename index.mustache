---
{
    "title": "Josemi Juanes - React Developer and Minimal Design Lover",
    "layout": "default.mustache",
    "permalink": "/index.html",
    "links": [
        {"text": "github", "url": "https://github.com/jmjuanes"},
        {"text": "linkedin", "url": "https://linkedin.com/in/jmjuanes"}
    ]
}
---
{{>header.mustache}}
{{>>content.mustache}}
<section class="mb-16 flex flex-col gap-4">
    <div class="text-2xl sm:text-3xl md:text-4xl leading-snug">
        <span>Hi 👋 I'm <b>Josemi</b>, a passionate <b>mathematician</b> turned <b>React</b> developer </span>
        <span>that loves to craft minimal, beautiful, and performant user interfaces.</span>
    </div>
    <div class="hidden text-2xl sm:text-3xl md:text-4xl leading-snug">
        <span>Currently I am building <a class="hover:underline font-bold" href="https://folio.josemi.xyz" target="_blank">folio</a>, a minimalistic digital whiteboard. </span>
        <span>You can also check the other <a class="hover:underline font-bold" href="/projects.html">projects</a> that I maintain.</span>
    </div>
    {{#if site.socials}}
    <div class="leading-snug flex items-center gap-2">
        {{#each site.socials}} 
        <a class="flex items-center gap-1 hover:underline decoration-4 underline-offset-2" href="{{this.url}}">
            <div class="text-2xl sm:text-3xl font-bold lowercase">{{this.text}}</div>
            <div class="flex items-center text-3xl sm:text-4xl">{{=icon icon="arrow-right"}}</div>
        </a>
        {{/each}}
    </div>
    {{/if}}
</section>
{{>>section.mustache title="Projects"}}
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
    {{#each site.data.projects limit=4}}
        <a href="{{this.url}}" class="group no-underline bg-gray-100 rounded-xl p-6">
            <div class="text-2xl font-bold group-hover:underline leading-none mb-2">{{this.title}}</div>
            <div class="text-base text-gray-700 mb-1">{{this.description}}</div>
            <div class="text-sm text-gray-500">{{this.url}}</div>
        </a>
    {{/each}}
    </div>
    <div class="mt-4">
        <a class="inline-flex items-center gap-1 lh-none no-underline hover:underline" href="/projects.html">
            <span>See all projects</span>
            {{=icon icon="arrow-right"}}
        </a>
    </div>
{{/section.mustache}}
{{>>section.mustache title="Notes"}}
    {{#pages collection="notes" limit=4}}
        {{>link.mustache url=path title=attributes.title description=attributes.description date=attributes.date}}
    {{/pages}}
    <div class="mt-4">
        <a class="inline-flex items-center gap-1 lh-none no-underline hover:underline" href="/notes.html">
            <span>See all notes</span>
            {{=icon icon="arrow-right"}}
        </a>
    </div>
{{/section.mustache}}
{{/content.mustache}}
{{>footer.mustache}}
